const express = require('express');
const userRequestRouter = express.Router();


module.exports = userRequestRouter; // Exporting the user request router
// This code defines a router for handling user request-related operations in an Express application.